language: python

python:
  - "2.7"

env:
  global:
    - KEYWORDS: [DATA_DEF_PORTAL_TAB_ID]

# command to install dependencies
install: pip install -r requirements.txt

# command to run tests
script: python travis_unit_test_script.py